import React, { FunctionComponent } from 'react'

import { ILayoutProps } from '@interfaces/layouts'

const EditorLayout: FunctionComponent<ILayoutProps> = ({ children }) => {
  return <>{children}</>
}

export default EditorLayout
